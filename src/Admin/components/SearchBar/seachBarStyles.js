import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  parent: {
      
  },
  searchbar:{
    paddingLeft: '1rem',
    paddingTop: '0.5rem'
  },
  searchbarinput: {
      borderRadius: '1rem'
  }
}));
